using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Acorn : MonoBehaviour
{
/*    Rigidbody2D rb;
    Guardian guardian;
    Player player;

    GameObject enemy;

    public Vector2 dir;
    float speed = 4;

    private void Start()
    {
        rb = GetComponent<Rigidbody2D>();
        guardian = GameObject.FindGameObjectWithTag("Guardian").GetComponent<Guardian>();
        player = GameObject.FindGameObjectWithTag("Character").GetComponent<Player>();
    }

    private void Update()
    {
        //enemy = player.FindClosestEnemy();
        GetDir();
    }

    private void FixedUpdate()
    {
        rb.velocity = dir * speed;
    }

    private Vector2 GetDir()
    {
        dir = (enemy.transform.localPosition - transform.localPosition).normalized;
        return dir;
    }*/
}
